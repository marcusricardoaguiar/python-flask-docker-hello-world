buildPack: none
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        steps:
        - sh: python -m unittest
          name: python-unittest
    release:
      build:
        steps:
        - sh: python -m unittest
          name: python-unittest