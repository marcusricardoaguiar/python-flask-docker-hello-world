buildPack: none
pipelineConfig:
  agent:
    label: jenkins-python-flask
    container: python
  pipelines:
    pullRequest:
      build:
        steps:
        - sh: python -m unittest
          name: python-unittest
    release:
      build:
        steps:
        - sh: python -m unittest
          name: python-unittest